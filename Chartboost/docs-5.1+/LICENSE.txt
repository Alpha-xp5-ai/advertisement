THIRD PARTY SOFTWARE DECLARATION FORM
======================================

SELLER NAME:               Alpha XP
SUBMITTED PLUGIN TITLE:    Alpha Ads - Chartboost Integration (UE 5.1+)

Does your submitted plugin use Third Party Software of any kind?
    YES

How many different sources of Third Party Software does your plugin use?
    1

------------------------------------------------------------
SOFTWARE #1
------------------------------------------------------------

NAME & VERSION:
    Chartboost SDK v6.6.0

SOFTWARE URL:
    https://www.chartboost.com
    Dashboard: https://dashboard.chartboost.com

COPY OF LICENSE:
    See ThirdPartyNotices.txt (bundled with plugin) for the full
    Chartboost SDK License text. Summary: royalty-free use by app
    publishers under the Chartboost Publisher Terms of Service;
    SDK redistribution as a standalone library is prohibited;
    required attribution included.

WHAT DOES THIS SOFTWARE DO?
    The Chartboost SDK delivers mobile advertising on Android. It
    requests, caches, and renders Banner, Interstitial, Rewarded
    Video, Native, and App Open ad formats from the Chartboost ad
    network, and reports impression / click / reward callbacks
    back to the host application.

WHY DOES YOUR PLUGIN NEED IT?
    The plugin is a UE Blueprint wrapper around the Chartboost
    SDK. Without the SDK, the plugin cannot request or display
    any ads. The SDK is the only component that communicates with
    Chartboost ad servers and renders ad views inside the Android
    Activity; the plugin only exposes its API to Blueprints.

IS THE LIBRARY STATICALLY OR DYNAMICALLY LINKED?
    Dynamically linked. The SDK ships as an Android .aar/.jar
    inside Source/ThirdParty/ChartboostSDK/ and is loaded at
    runtime by the Android ART runtime via the application
    classpath. It is NOT statically linked into the UE .so
    binaries.

WILL THIS SOFTWARE SEND DATA BACK TO THE CREATOR?
    YES. The Chartboost SDK transmits data to Chartboost servers,
    including:
        - Anonymous device advertising identifier (Android AAID),
          subject to user consent
        - Device model, OS version, locale, network type
        - Ad request, impression, click, completion, and reward
          events
        - SDK crash and diagnostic telemetry
    Data collection is governed by the Chartboost Privacy Policy:
        https://answers.chartboost.com/en-us/articles/200780269
    The plugin exposes a Blueprint consent API (GDPR / CCPA /
    COPPA) so the host application can gate or disable this data
    collection on a per-user basis.
